[user]
	name = Pranav Sharma
	email = pranav.sharma.ama@gmail.com
[credential]
    helper = cache --timeout 21600
    {{- if eq .chezmoi.hostname "pranav-xelite" }}
    helper = oauth -device
    {{- else }}
	helper = /usr/libexec/git-core/git-credential-libsecret
    {{- end }}
[merge]
        tool = vimdiff
        conflictstyle = diff3
[pull]
	rebase = true
